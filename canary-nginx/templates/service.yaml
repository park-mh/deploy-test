apiVersion: v1
kind: Service
metadata:
  name: {{ include "canary-nginx.fullname" . }}
  labels:
    {{- include "canary-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "canary-nginx.selectorLabels" . | nindent 4 }}
