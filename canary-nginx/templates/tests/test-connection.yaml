apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canary-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "canary-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canary-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
